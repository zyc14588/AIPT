# 里程碑 M0（MILESTONE M0）

> M0 里程碑合同。批次串行顺序以 [../authority/BATCH_DEPENDENCY_GRAPH.md](../authority/BATCH_DEPENDENCY_GRAPH.md) 为准。

## M0 目标

M0 建立**可构建、可验证的工程基础**，不实现真实桌测（`R12-Q015`）。M0 采用串行小批次
`AIPT-M0-B000`–`AIPT-M0-B008` 与游戏 P0 / 集成任务（`R12-Q016`）。

## 串行批次图

```text
AIPT-M0-B000 → AIPT-M0-B001 → AIPT-M0-B002
  → UNREGISTERED-AIPT-P0-B000 → UNREGISTERED-AIPT-P0-B001
  → AIPT-M0-B003 → AIPT-M0-B004 → AIPT-M0-B005 → AIPT-M0-B006
  → UNREGISTERED-AIPT-P0-B002 → UNREGISTERED-AIPT-P0-B003
  → AIPT-M0-B007 → INT-AIPT-UNREGISTERED-001（只读）→ AIPT-M0-B008
```

施工规则为单批次单仓库且 `GLOBAL_WIP` 最多为 1；前一批次正式关闭后才可由新 Owner Authority 启动下一批次，集成任务只读两个来源 Commit。M0 closeout 后当前 `GLOBAL_WIP = 0`，没有活动批次。

## B000：只安装权威文档

`AIPT-M0-B000` 的范围**只**包括：

- 公共权威文档（章程、治理、决策矩阵、状态、批次图）；
- 机器决策登记（454 条决策、覆盖关系、延期参数、项目状态）；
- MIT License 与权威入口 README。

B000 **不**实现运行时代码、不建立 CI、不修改《未登记》。

## B000 无 CI：一次性 Bootstrap 例外

- 批次普遍要求 CI，但 B000 在公共 CI 尚不存在时启动，使用 `BOOTSTRAP-Q001` 一次性例外：
  **最终本地确定性验收 + GPT 治理/文档审计** 关闭（`BOOTSTRAP-Q001`）。
- `AIPT-M0-B001` 建立无秘密公共 CI（`R12-Q019`）后，第一次 CI 必须**追溯验证** B000 的文档、JSON、链接与 MIT License。
- 该例外**不适用**于 B001 之后的普通批次。

## M0 Development Pass 与生产资格分离

- `AIPT-M0-B008` 绑定已完成的 M0 综合验收与 GPT Hard Gate（`PASS`），final Candidate 已由 exact implementation merge 集成并完成 [M0 Development Pass](M0_DEVELOPMENT_PASS.md) closeout（`R12-Q018`、`R14-Q024`）。`AIPT-M0-B008 = MERGED_CLOSED`，`M0 Development Pass = GRANTED`，现在正式生效；M0 交付的是可构建、可验证的工程基础。
- Development Pass **不是**生产/发布资格：生产/发行需要双审计（GPT + Claude）、完整证据资格与签名（`R10-F002`、`R13-Q024`、`R9-F003`、`R9-Q015`、`R10-Q016`）。
- 开发 Break-glass 产物无生产/发行资格（`R16-Q004`）。
- M0 未执行真实 TRPG 桌测，不授予 MVP Development Pass，不作 human-equivalence 声明；生产资格与发行资格均未授予，第二审计者生产门仍未完成，MODEL/HARNESS/IPC 生产 gate 仍未实现。平台集成继续为 `FROZEN_WAITING_M1_ENGINE`，没有自动下一批次；后续工作必须由新的 Owner Authority 另行启动。

## 相邻文档

- [../authority/README.md](../authority/README.md) · [../authority/PROJECT_STATUS.md](../authority/PROJECT_STATUS.md) · [../authority/BATCH_DEPENDENCY_GRAPH.md](../authority/BATCH_DEPENDENCY_GRAPH.md) · [M0 Development Pass](M0_DEVELOPMENT_PASS.md) · [MVP.md](MVP.md)
- [返回仓库首页](../../README.md)
