# AIPT-MVP-B005 Authority Matrix

This matrix records the authority recovery performed before B005 implementation. The exact authorized base is `176f33d8f20f94a77ab688f4869e944b6ffe97c6` (tree `210320957a35633bcf766a3d88ea50a3493bd0fc`). Its direct serial predecessor is the closed read-only integration task `INT-AIPT-UNREGISTERED-MVP-001`; the canonical closeout record has SHA-256 `1f22028561c90619755314eedb50869bb40e78b4ef55458e35eb654bf8d9ebc2`. No integration rerun is part of B005.

| Requirement | Authority / contract | Existing implementation at Base | Gap | B005 artifact | Acceptance probe |
|---|---|---|---|---|---|
| Three-stage evidence pipeline and machine-authoritative manifests | `R9-Q002`, `R9-F001`, `R10-Q001`, `R10-Q002` | Strict `aipt.evidence/v1` Schema; native `RAW_CAPTURE` only | No `AUDIT_READY` runtime | `audit_ready.go`, additive schemas | Generate then independently verify `stage = AUDIT_READY`; reject `AUDIT_RESULT` confusion |
| Read-only audit preparation | `R10-Q005`, `R11-Q003`, `R4-Q024` | RAW capture reads a verified ledger snapshot | No offline audit surface | `cmd/aipt-audit-ready`, `GitMirrorVerifier` | Static capability gate plus source/input identity checks before and after normalization |
| Immutable pushed source identity | `R9-Q003`, `R9-Q004`, `R11-Q006`, `R11-Q012` | Manifest records repository/commit/tree but RAW verifier does not consult Git | Commit/tree not independently resolved | Descriptor-held local bare-mirror verifier | Synthetic bare remote; wrong repository, commit, and tree reject |
| Verify native evidence before normalization | `R10-Q004`, `R10-Q007`, `R10-F002` | Independent fail-closed `VerifyRawCapture` | Not composed into `AUDIT_READY` | Held RAW material acquisition and stability checks | Existing full RAW tamper table plus generation input-race probes |
| Run evidence closure | `R9-Q008`, `R9-Q010`, `R10-Q010`, `R10-Q011` | Run Manifest, ledger, Run Core, orchestration identities exist separately | No single closure contract | `aipt.run-evidence-closure/v1` | Cross-bind manifest, source, ledger tail, receipts, projection, Rules, coverage, defects, gates, and model references |
| PostgreSQL remains state authority | `R2-Q011`, `R4-Q007`, `R4-Q008`, `R4-Q009` | Append-only PostgreSQL hash-chain ledger | Reports could otherwise be mistaken for state | Exact `POSTGRESQL_APPEND_ONLY_HASH_CHAIN` field and RAW binding | Derived projection/report cannot replace ledger identity |
| Structured replay evidence | `R2-Q017`, `R8-Q003`, `R9-Q010` | Deterministic Run Core replay | No portable evidence contract | `aipt.replay-evidence/v1` | Live/replayed final hashes must be equal and `hash_match` must be true |
| RNG evidence | Existing B002 RNG and seed-commitment semantics; `R8-Q003` | Versioned RNG and commitments | Not represented in evidence closure | Exact used / unused forms in closure and replay | No-RNG exact representation and RNG commitment/disclosure validation |
| Independent defect dimensions | `R3-Q001`, `R3-Q002` | Authority freezes root-cause domains and independent dimensions | No machine defect contract | Family and occurrence schemas/types | Root cause, severity, confidence, reproducibility, scope, and priority validate independently |
| Family versus occurrence | `R8-Q018`, `R8-Q019`, `R8-Q020` | Decision only | No implementation | Versioned canonical fingerprint and separate family/occurrence records | Exact fingerprint groups; a different fingerprint yields only `SEMANTIC_DUPLICATE_CANDIDATE` |
| Defect administration history | `R3-Q005`, `R8-Q023`, `R11-Q024` | Authority requires a state machine and append-only decisions but freezes no state-name set | Must not invent top-level state names | Authority-supplied versioned state graph plus hash-linked decision chain | Undeclared/terminal/invalid transition and broken predecessor reject; B005 defines no built-in governance state names |
| Canonical report and derivatives | `R8-Q021`, `R9-Q020` | Report formats named by Authority | No Run report contract/renderers | Canonical JSON plus Markdown, CSV, JUnit, and static HTML renderers | Verifier regenerates every derivative byte from canonical JSON |
| Report lifecycle | `R8-Q022` | Exact lifecycle frozen | No transition enforcement | `PROVISIONAL → FINALIZING → SEALED`; append-only addendum contract | Invalid transition, in-place mutation, and any implicit unseal reject |
| No false audit verdict | `R9-F001`, `R9-F004`, `R10-Q007` | `AUDIT_RESULT` Schema exists; no generator | Valid evidence could be mislabeled PASS | Explicit report claim/result boundary | `AUDIT_READY` never emits an audit result or auditor verdict |
| Disclosure profiles | `R2-Q020`, `R4-F002`, `R9-Q013`, `R9-Q014`, `R11-Q016` | Profiles exist in the root Schema | No content-aware export enforcement | Typed classification/kind policy and conservative high-confidence marker scan | PUBLIC rejects unpublished, credential, private-prompt, private-locator, game-body, and local-path material |
| Encryption boundary | `R4-Q018`, `R10-Q012`, `R10-Q013` | Schema requires encryption for applicable profiles; no approved B005 crypto implementation | Plaintext fallback risk | Typed `ENCRYPTION_REQUIRED_BUT_UNAVAILABLE` refusal | External unpublished and all private-full generation fail before writing |
| Deterministic normalization | `R10-Q003`, `R9-Q016` | RAW capture deterministic | No `AUDIT_READY` normalization/root | Canonical JSON, sorted sets/assets, deterministic renderers/index | Reordered inputs produce identical file set, bytes, manifest, asset hashes, and root |
| Oversized evidence | `R9-Q012`, `R10-Q009`, `R10-Q018`, `R14-F002` | Not implemented | Required content could be truncated | Deployment-profile-driven SHA-256 chunking/deduplication | Missing, reordered, substituted, truncated, extra, or excessive chunks reject; reassembly is byte exact |
| Deployment thresholds | `DEFER-006`, `R10-Q009` | No production values frozen | Must avoid invented business constants | Required `ExportProfile` operational input bounded by absolute safety caps | Tiny synthetic thresholds force chunk route; invalid limits reject |
| Filesystem safety and TOCTOU | `R4-Q014`, `R11-Q005`, `R11-Q006`, `R11-Q014` | RAW exporter has private sibling, held descriptors, fsync, and no-replace publish | Not available to `AUDIT_READY` | Reused descriptor/no-follow/no-replace primitives plus input snapshot and repeated verification | Existing target, symlink, path escape, changed RAW/source/normalized input, and partial output reject |
| Untrusted-data boundary | `R11-Q002`, `R11-Q011`, `R11-Q013` | Existing bounded parsers | New report/evidence inputs are untrusted | Strict decoding, closed schemas, bounded files/assets/chunks, fixed Git argv, stable error codes | Unknown fields, resource overflows, Git identity injection, and error-content leaks reject |
| Public CI and synthetic database smoke | `R4-Q022`, `R10-F002`, toolchain lock | PostgreSQL 18.4 RAW integration exists | No B005 closure path | B005 validator, Go integration test, workflow steps | Ephemeral loopback PostgreSQL 18.4 → RAW → closure/replay/report → repeated verified `AUDIT_READY` |
| Qualification/runtime boundaries | Batch graph; existing B004 closeout and qualification gates | Runtime remains blocked at IPC; qualification counts are zero | B005 could accidentally broaden scope | Status projection and validator assertions | Real model/provider calls 0; playtest false; qualification 0; runtime not ready; B006 not started |

The legacy `aipt.evidence/v1` document and minimal RAW fixture remain byte-identical. B005 adds referenced contract documents rather than changing the meaning of any existing v1 field. Signing and encryption implementations, `AUDIT_RESULT` generation, operational Web UI, real-model diagnostics, and qualification runs remain outside this batch.
