// Package core provides the B004 lifecycle shell for the future AIPT action
// core. It owns lifecycle state and injected readiness dependencies only. It
// opens no listener, performs no model call, and implements no action-pipeline
// semantics.
package core
